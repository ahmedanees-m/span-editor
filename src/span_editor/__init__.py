"""SPAN: a geometric model of effector tethering in R-loop genome editors.

Submodules are imported directly, for example:

    from span_editor.model import SpanModel, build_context
    from span_editor.occupancy import ModelParameters
"""

__version__ = "0.1.0"
